# Полный override config_file: без listen_addresses Postgres слушает только localhost —
# тогда другие контейнеры (api) получают Connection refused.
listen_addresses = '*'

max_connections = 1000
shared_buffers = 512MB
logging_collector = on
log_statement = 'all'
